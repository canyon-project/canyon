---
title: API Overview
overview: true
---

HTTP API for the Canyon coverage server. Base URL defaults to `http://localhost:3000`.

All coverage endpoints accept `application/json` and return JSON. CORS is enabled for `/api/*`.
