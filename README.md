## eslint
前后端使用vite默认+canyon原有配置，一切走默认

## 覆盖率聚合

1.使用扁平化的数据结构，map和hit分开存储，方便后续的聚合
2.先存，后聚合，聚合后删除原有数据

不同build的map是否相同？存疑虑

## bun
尝试bun，主要想使用它的bun run src/main.ts
https://github.com/letstri/bun-nestjs

如何确保数据准确合并？

不同端不建议合并。
原因：不同端一套代码，理论上可以合并，但是实际上，不同端的构建流程可能不一样，对于相同一份代码，可能编译的结果不一样，这样合并后的数据就不准确了。


覆盖率落库流程

1.map和hit分开存储
2.map判断path是否存在，不存在则存储
3.分开插入不同的表中
