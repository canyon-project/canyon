# Canyon Examples

This repository is an independent pnpm workspace containing multiple example projects. It can run independently and is not affected by the external framework.

## Usage

```shell
pnpm install
```

This will install all dependencies for all examples.
