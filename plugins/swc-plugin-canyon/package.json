{
  "name": "swc-plugin-canyon",
  "version": "0.0.2-alpha.8",
  "description": "Canyon SWC plugin, for debugging",
  "main": "swc_plugin_canyon.wasm",
  "scripts": {
    "prepack": "cargo build --release -p swc_plugin_canyon --target wasm32-wasi && cp ../../target/wasm32-wasi/release/swc_plugin_canyon.wasm . && ls && cat package.json"
  },
  "devDependencies": {
    "@swc/cli": "0.4.1-nightly.20240914",
    "@swc/core": "1.6.13",
    "swc-plugin-coverage-instrument": "^0.0.24"
  }
}
