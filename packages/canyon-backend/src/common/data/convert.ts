// 数据库单个覆盖率文件的branchMap、fnMap、statementMap的zstd转成对象
export const dbSignFileMapZstdStrToObj = (str: string): any => {
  // return JSON.parse(zstd.decompress(Buffer.from(str, "base64")).toString());
};
