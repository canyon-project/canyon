mutation FavorProject($projectID: ID!,$favored: Boolean!) {
  favorProject(projectID: $projectID,favored: $favored) {
    id
    username
    password
    nickname
    avatar
    refreshToken
    accessToken
    email
    favor
    createdAt
  }
}
