query GetProjectByID($projectID: ID!) {
  getProjectByID(projectID: $projectID) {
    id
    name
    pathWithNamespace
    description
    reportTimes
    lastReportTime
    coverage
    tag
    branchOptions
    defaultBranch
  }
}
