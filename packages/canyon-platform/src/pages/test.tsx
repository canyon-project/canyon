// 对于shiki，我们需要一个组件，直接可以着色

// 参数，fileCoverage、fileContent、

function fn(fileCoverage, fileContent) {
  return {
    lines: [], //行信息
  };
}

const Test = () => {
  return <div>Test</div>;
};

export default Test;
